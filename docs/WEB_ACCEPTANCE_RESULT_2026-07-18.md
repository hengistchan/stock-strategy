# Web 工作台验收结果（2026-07-18）

> 后续兼容性审计说明：本页作业产生于 `engine_contract v2` 之前，保留为历史工程验收记录。当前 Web 会将其标记为 `LEGACY RESULT`；需要使用新引擎重新运行后，才可比较撮合和策略指标。`K_5M / HFQ` 增量作业只验证价格图读取，并不验证声明 `K_DAY` 的示例策略语义。

## 结论

本机 Web MVP 已通过自动化、真实 OpenD 回测、浏览器交互、价格图、刷新恢复、服务重启恢复和移动宽度验收。

服务地址：`http://127.0.0.1:8000`

## 产品边界

- 本机单用户，不提供登录和公网部署。
- 所有市场行情固定从 OpenD 获取；没有网页价格或模拟行情回退。
- 只允许运行 `examples/` 和 `strategies/` 下的 Python 策略。
- 用户策略在独立子进程中执行，默认 300 秒超时。
- 不读取交易账户，不发送订单。
- 作业状态、请求参数和运行目录写入 `runs/web/<任务 ID>/job.json`。
- 每次 OpenD 原始响应独立缓存到 `data/opend/web/`。

## 验收作业

- 作业 ID：`20260718-003223-09bb6d`
- 策略：`examples/ma_cross.py`
- 标的：`US.AAPL`
- 区间：`2024-01-01` 至 `2025-12-31`
- K 线与复权：`K_DAY`、`QFQ`
- 初始资金：100,000
- 佣金：3 bps；最低佣金：1；滑点：5 bps
- OpenD 有效 K 线：502
- 状态：`succeeded`

产物：

- [作业记录](../runs/web/20260718-003223-09bb6d/job.json)
- [回测汇总](../runs/web/20260718-003223-09bb6d/output/20260718-003225-ma_cross/summary.json)
- [成交记录](../runs/web/20260718-003223-09bb6d/output/20260718-003225-ma_cross/trades.csv)
- [资金曲线](../runs/web/20260718-003223-09bb6d/output/20260718-003225-ma_cross/equity_curve.csv)
- [图表报告](../runs/web/20260718-003223-09bb6d/output/20260718-003225-ma_cross/report.svg)

页面指标与 CLI/JSON 一致：策略收益 `+52.17%`、买入持有 `+47.83%`、最大回撤 `-12.73%`、Sharpe `1.41`、闭合交易 `3`、总费用 `200.27`。

## 自动化验收

`python3 backend/scripts/acceptance.py`：

- 23 项单元与 API 测试通过。
- OpenD 同字段结构工程验收通过。
- Python 与 JavaScript 语法检查通过。
- 项目虚拟环境依赖检查无冲突。

## 浏览器验收

使用 `agent-browser` 验证：

1. 首页、CSS、JavaScript、健康状态和策略配置均成功加载。
2. 页面有实际内容，无框架错误层，`window.__consoleErrors` 为空。
3. 通过页面提交真实 OpenD 回测，异步状态正常转换为成功。
4. 报告 SVG 加载成功，指标和三笔成交正确渲染。
5. 价格图读取同一作业的 OpenD 缓存，蜡烛、成交量、MA20/MA60 和进出场位置正常渲染。
6. `3M / 6M / 1Y / ALL` 日线区间与分钟线 `63根 / 126根 / 252根 / 全部` 区间可以切换。
7. 分钟线悬停会显示精确到分钟的 OHLC、涨跌幅和成交量。
8. 刷新页面后自动恢复最近成功作业。
9. 完整停止并重启 Web 服务后，作业历史与结果仍能恢复。
10. 桌面宽度无横向溢出。
11. `390 × 844` 移动宽度无页面级横向溢出，价格读数、图表、指标、报告和成交账簿可读。

## 价格图增量验收

使用作业 `20260718-004401-b8cd84` 的真实 OpenD `US.AAPL / K_5M / HFQ` 缓存验证，共 `58,368` 根 K 线。页面悬停到 `2025-12-31 14:15` 时显示：开 `63,400.88`、高 `63,405.36`、低 `63,366.16`、收 `63,391.92`、成交量 `719`；逐项与该作业缓存 CSV 一致。桌面与移动宽度均无页面级横向溢出，浏览器错误收集为空。

历史表现只用于研究和机制验证，不构成买卖建议；三笔交易不足以证明策略稳健。
