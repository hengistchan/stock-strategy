# Security Policy

## 支持范围

当前维护分支为默认分支。安全修复不承诺回移到旧版本。

## 报告漏洞

请使用 GitHub Security Advisory 的私密报告功能，不要先创建公开 Issue。报告应包含影响、复现步骤、受影响文件与建议修复方向。

## 部署边界

Strategy Lab 默认只绑定 `127.0.0.1`，没有认证、租户隔离或公网防护。不要把 Web 服务直接暴露到公网。策略是本地 Python 代码，运行回测等同于允许该脚本在本机回测子进程中执行；只运行你信任的策略文件。
